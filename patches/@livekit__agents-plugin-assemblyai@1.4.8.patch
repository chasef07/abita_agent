diff --git a/dist/stt.cjs b/dist/stt.cjs
index d543685..244701b 100644
--- a/dist/stt.cjs
+++ b/dist/stt.cjs
@@ -28,6 +28,14 @@ const U3_PRO_MODELS = ["u3-rt-pro", "u3-rt-pro-beta-1", "universal-3-5-pro"];
 function isU3ProModel(model) {
   return U3_PRO_MODELS.includes(model);
 }
+function speechDataMetadata(data) {
+  const assemblyai = {};
+  if (typeof data.language_confidence === "number") {
+    assemblyai.languageConfidence = data.language_confidence;
+  }
+  if (Object.keys(assemblyai).length === 0) return void 0;
+  return { assemblyai };
+}
 const defaultSTTOptions = {
   apiKey: process.env.ASSEMBLYAI_API_KEY,
   sampleRate: 16e3,
@@ -198,6 +206,7 @@ class SpeechStream extends import_agents.stt.SpeechStream {
       encoding: this.#opts.encoding,
       speech_model: this.#opts.speechModel,
       format_turns: this.#opts.formatTurns,
+      inactivity_timeout: this.#opts.inactivityTimeout,
       end_of_turn_confidence_threshold: this.#opts.endOfTurnConfidenceThreshold,
       min_turn_silence: minSilence,
       max_turn_silence: maxSilence,
@@ -367,6 +376,7 @@ class SpeechStream extends import_agents.stt.SpeechStream {
     const utterance = data.utterance ?? "";
     const transcript = data.transcript ?? "";
     const language = (0, import_agents.normalizeLanguage)(data.language_code ?? "en");
+    const metadata = speechDataMetadata(data);
     const timedWords = words.map(
       (word) => (0, import_agents.createTimedString)({
         text: word.text ?? "",
@@ -393,7 +403,8 @@ class SpeechStream extends import_agents.stt.SpeechStream {
             startTime,
             endTime,
             confidence,
-            words: timedWords
+            words: timedWords,
+            ...(metadata ? { metadata } : {})
           }
         ]
       });
@@ -415,7 +426,8 @@ class SpeechStream extends import_agents.stt.SpeechStream {
             startTime: this.#lastPreflightStartTime,
             endTime,
             confidence: utteranceConfidence,
-            words: utteranceWords
+            words: utteranceWords,
+            ...(metadata ? { metadata } : {})
           }
         ]
       });
@@ -432,7 +444,8 @@ class SpeechStream extends import_agents.stt.SpeechStream {
             startTime,
             endTime,
             confidence,
-            words: timedWords
+            words: timedWords,
+            ...(metadata ? { metadata } : {})
           }
         ]
       });
diff --git a/dist/stt.d.cts b/dist/stt.d.cts
index 36dadbb..52bb11b 100644
--- a/dist/stt.d.cts
+++ b/dist/stt.d.cts
@@ -12,6 +12,8 @@ export interface STTOptions {
     encoding: STTEncoding;
     speechModel: STTModels;
     languageDetection?: boolean;
+    /** Time in seconds of inactivity before AssemblyAI terminates the session. */
+    inactivityTimeout?: number;
     endOfTurnConfidenceThreshold?: number;
     /** Minimum silence (ms) before a confident end-of-turn is finalized. */
     minTurnSilence?: number;
diff --git a/dist/stt.d.ts b/dist/stt.d.ts
index 36dadbb..52bb11b 100644
--- a/dist/stt.d.ts
+++ b/dist/stt.d.ts
@@ -12,6 +12,8 @@ export interface STTOptions {
     encoding: STTEncoding;
     speechModel: STTModels;
     languageDetection?: boolean;
+    /** Time in seconds of inactivity before AssemblyAI terminates the session. */
+    inactivityTimeout?: number;
     endOfTurnConfidenceThreshold?: number;
     /** Minimum silence (ms) before a confident end-of-turn is finalized. */
     minTurnSilence?: number;
diff --git a/dist/stt.js b/dist/stt.js
index 0c60f2b..2645696 100644
--- a/dist/stt.js
+++ b/dist/stt.js
@@ -14,6 +14,14 @@ const U3_PRO_MODELS = ["u3-rt-pro", "u3-rt-pro-beta-1", "universal-3-5-pro"];
 function isU3ProModel(model) {
   return U3_PRO_MODELS.includes(model);
 }
+function speechDataMetadata(data) {
+  const assemblyai = {};
+  if (typeof data.language_confidence === "number") {
+    assemblyai.languageConfidence = data.language_confidence;
+  }
+  if (Object.keys(assemblyai).length === 0) return void 0;
+  return { assemblyai };
+}
 const defaultSTTOptions = {
   apiKey: process.env.ASSEMBLYAI_API_KEY,
   sampleRate: 16e3,
@@ -184,6 +192,7 @@ class SpeechStream extends stt.SpeechStream {
       encoding: this.#opts.encoding,
       speech_model: this.#opts.speechModel,
       format_turns: this.#opts.formatTurns,
+      inactivity_timeout: this.#opts.inactivityTimeout,
       end_of_turn_confidence_threshold: this.#opts.endOfTurnConfidenceThreshold,
       min_turn_silence: minSilence,
       max_turn_silence: maxSilence,
@@ -353,6 +362,7 @@ class SpeechStream extends stt.SpeechStream {
     const utterance = data.utterance ?? "";
     const transcript = data.transcript ?? "";
     const language = normalizeLanguage(data.language_code ?? "en");
+    const metadata = speechDataMetadata(data);
     const timedWords = words.map(
       (word) => createTimedString({
         text: word.text ?? "",
@@ -379,7 +389,8 @@ class SpeechStream extends stt.SpeechStream {
             startTime,
             endTime,
             confidence,
-            words: timedWords
+            words: timedWords,
+            ...(metadata ? { metadata } : {})
           }
         ]
       });
@@ -401,7 +412,8 @@ class SpeechStream extends stt.SpeechStream {
             startTime: this.#lastPreflightStartTime,
             endTime,
             confidence: utteranceConfidence,
-            words: utteranceWords
+            words: utteranceWords,
+            ...(metadata ? { metadata } : {})
           }
         ]
       });
@@ -418,7 +430,8 @@ class SpeechStream extends stt.SpeechStream {
             startTime,
             endTime,
             confidence,
-            words: timedWords
+            words: timedWords,
+            ...(metadata ? { metadata } : {})
           }
         ]
       });
diff --git a/src/stt.ts b/src/stt.ts
index a774b44..bdbcd26 100644
--- a/src/stt.ts
+++ b/src/stt.ts
@@ -40,6 +40,7 @@ interface StreamEventMessage {
   end_of_turn_confidence?: number;
   turn_is_formatted?: boolean;
   language_code?: string;
+  language_confidence?: number;
   speaker_label?: string;
   words?: Array<{
     text?: string;
@@ -53,6 +54,18 @@ interface StreamEventMessage {
   session_duration_seconds?: number;
 }
 
+function speechDataMetadata(data: StreamEventMessage): stt.SpeechData['metadata'] | undefined {
+  const assemblyai: Record<string, number> = {};
+
+  if (typeof data.language_confidence === 'number') {
+    assemblyai.languageConfidence = data.language_confidence;
+  }
+
+  if (Object.keys(assemblyai).length === 0) return undefined;
+
+  return { assemblyai };
+}
+
 export interface STTOptions {
   apiKey?: string;
   sampleRate: number;
@@ -65,6 +78,8 @@ export interface STTOptions {
   encoding: STTEncoding;
   speechModel: STTModels;
   languageDetection?: boolean;
+  /** Time in seconds of inactivity before AssemblyAI terminates the session. */
+  inactivityTimeout?: number;
   endOfTurnConfidenceThreshold?: number;
   /** Minimum silence (ms) before a confident end-of-turn is finalized. */
   minTurnSilence?: number;
@@ -302,6 +317,7 @@ export class SpeechStream extends stt.SpeechStream {
       encoding: this.#opts.encoding,
       speech_model: this.#opts.speechModel,
       format_turns: this.#opts.formatTurns,
+      inactivity_timeout: this.#opts.inactivityTimeout,
       end_of_turn_confidence_threshold: this.#opts.endOfTurnConfidenceThreshold,
       min_turn_silence: minSilence,
       max_turn_silence: maxSilence,
@@ -508,6 +524,7 @@ export class SpeechStream extends stt.SpeechStream {
     const utterance = data.utterance ?? '';
     const transcript = data.transcript ?? '';
     const language = normalizeLanguage(data.language_code ?? 'en');
+    const metadata = speechDataMetadata(data);
 
     // Word timestamps are in milliseconds:
     // https://www.assemblyai.com/docs/api-reference/streaming-api/streaming-api#receive.receiveTurn.words
@@ -542,6 +559,7 @@ export class SpeechStream extends stt.SpeechStream {
             endTime,
             confidence,
             words: timedWords,
+            ...(metadata ? { metadata } : {}),
           },
         ],
       });
@@ -569,6 +587,7 @@ export class SpeechStream extends stt.SpeechStream {
             endTime,
             confidence: utteranceConfidence,
             words: utteranceWords,
+            ...(metadata ? { metadata } : {}),
           },
         ],
       });
@@ -589,6 +608,7 @@ export class SpeechStream extends stt.SpeechStream {
             endTime,
             confidence,
             words: timedWords,
+            ...(metadata ? { metadata } : {}),
           },
         ],
       });
