# Optimization Run: 2026-04-02

## Transcripts Evaluated
- SCL_vRhteLJ3JeQi: new_patient (Spanish), confusion loop — phone number collection in Spanish (118 turns)
- SCL_pBpSSHAS8VKU: transfer, missed intent + double transfer_call (18 turns)
- SCL_XQuhUmDAvowr: new_patient, clean — successful registration + booking (57 turns)
- SCL_NLZya5NUF5TG: new_patient, caller said "representative" twice but agent continued (40 turns)
- SCL_VFDwKT4Gj8Lp: new_patient, CRITICAL — fabricated registration data (41 turns)
- SCL_Xqttbmkd28RA: faq, clean — insurance check for UHC rep (17 turns)
- SCL_5FgJ9jeTcKpX: faq, clean — Optimum not accepted, handled well (12 turns)
- SCL_aUbCS7t4ewyd: transfer, returning a call — double transfer_call (7 turns)
- SCL_ZJc62oinGuJE: transfer, authorization request — double transfer_call (7 turns)
- SCL_yAw5W2GafiK2: transfer, caller insists "representative" — double transfer_call (15 turns)
- SCL_qoZu4HFco3za: faq, clean — quick address lookup (4 turns)
- SCL_f8u6so3rTz9q: transfer, medical records request — double transfer_call (3 turns)
- SCL_VdRhCvQaLAb7: existing_patient, evaluated (6 turns)
- SCL_tvo89u64KP9o: transfer, evaluated (3 turns)
- SCL_Zt74n8u6VJVn: transfer, 4x transfer_call (3 turns)
- SCL_rtYQTGDoq6DS: evaluated, short (5 turns)
- SCL_UMctMuDSMErR: evaluated (5 turns)
- SCL_PbVHwnY9tMMA: transfer, double transfer_call (3 turns)
- SCL_YB4Jydf5V6MB: evaluated (6 turns)
- SCL_pC7PZjLiEkyp: transfer, double transfer_call (4 turns)

## Issues Found
1. **Fabricated registration data** — SCL_VFDwKT4Gj8Lp
   Agent called add_patient with hallucinated email, address, and member ID. Skipped entire collection phase.
   Root cause: LLM filled required tool parameters with made-up data to satisfy the schema. No prompt rule explicitly forbade this.

2. **Double transfer_call** — 46 of 195 total calls (24%)
   transfer_call called 2+ times per call. 3 calls had 4 calls. SIP disconnects after first, so extras are wasted.
   Root cause: LLM generates multiple tool calls before SIP disconnect is processed. Existing "do not retry" instruction was insufficient.

3. **Transfer-on-insistence ignored** — SCL_NLZya5NUF5TG
   Caller said "Live representative" and "Agent" but agent continued registration flow.
   Root cause: RUNBOOK rule was too vague ("if they insist after one ask"). Didn't enumerate trigger words.

## Changes Made
1. RUNBOOK.md (Path 2): Added "collect every field before calling add_patient" guard
   Why: Prevents fabricated/hallucinated registration data

2. RUNBOOK.md (Path 4): Strengthened transfer-on-insistence with explicit trigger words
   Why: Agent was ignoring "representative"/"agent" as insistence signals

3. tools.ts (transfer_call description): Added "SIP session disconnects — no further tool calls or text"
   Why: LLM needs to understand that generation must stop after this tool

4. tools.ts (add_patient description): Added "NEVER call with fabricated/guessed data"
   Why: Direct guard against the hallucination behavior observed

## Skipped Issues
- Spanish phone number confusion loop (SCL_vRhteLJ3JeQi): Complex bilingual interaction issue. Agent struggled to collect digits in Spanish across 10+ turns. Fixable but requires careful VOICE.md changes for bilingual digit handling — deferred to next run.
- Double transfer_call: Prompt-level fix may not fully resolve this. A code-level guard (check state.transferred before executing) would be more reliable. Noted in CHANGELOG.
