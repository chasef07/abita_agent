# Optimization Run: 2026-04-04

## Transcripts Evaluated
- SCL_GTugHgdBqz3P: faq, clean — Simply insurance check, STT struggled but agent eventually understood (11 turns)
- SCL_sWHFvhqrWfQG: faq, clean — test call, insurance/glasses/cataracts questions (17 turns)
- SCL_LL5rU6w6ZTWQ: new_patient, clean — TierCare Plus not accepted, correctly handled unknown insurance (9 turns)
- SCL_aQJVHVT7yu9s: existing_patient, clean — verified patient, caller asked for representative, transferred. Double transfer_call (9 turns)
- SCL_yNq4S5vavVtL: faq, clean — Simply Health Plans not accepted, called from iCare rep (10 turns)
- SCL_dJShCrryEKEj: existing_patient, clean — appointment confirmation (9 turns)
- SCL_7eNUiNAGuNAX: faq + transfer, clean — Curative Insurance not accepted, transferred. Double transfer_call (9 turns)
- SCL_qgMJRwgEb4Qi: transfer, missed intent — caller said "a referral" but agent tried to schedule instead of transferring (5 turns)
- SCL_a3aJmtBwS9qr: existing_patient, clean — reconnect after disconnect, AI disclosure, transfer. Double transfer_call (17 turns)
- SCL_yakDdFcABvPY: existing_patient, clean — appointment confirmation + location check (9 turns)
- SCL_ouFMNPtDjYPM: skipped — disjointed/confused caller, no real interaction (11 turns)
- SCL_ibqfNmgbWxqh: new_patient (Spanish), clean — registration for family member, booking on same day as existing appointment (57 turns)
- SCL_yJjZFyKhxF6A: transfer, incomplete — returning call, patient lookup failed, caller switched to Spanish (10 turns)
- SCL_B8MWYb2dfrf5: new_patient, stale availability — registration successful, but 3 consecutive booking failures before 4th slot worked (52 turns)
- SCL_dWDVsoJsiG9F: robocall — Google Voice business verification, skipped (4 turns)
- SCL_R8bZsBSdFH9K: new_patient, borderline — caller said "can I speak to someone" at turn 42 due to interruption frustration, but continued and completed registration+booking (67 turns)
- SCL_FiTuW8kYQgYW: transfer, clean — caller wanted to change appointment urgently, agent tried to help then transferred on insistence. Double transfer_call (7 turns)
- SCL_DRxTQWN62L3U: robocall — Google Voice business verification, skipped (10 turns)
- SCL_QKrZ8s4vvbyS: existing_patient, clean — scheduling with Dr. Bach, correct day-of-week handling (18 turns)
- SCL_ZRH9zcisbPPw: transfer, clean — emergency appointment change, agent tried to help then transferred. Double transfer_call (5 turns)

## Issues Found
1. **Referral treated as scheduling** — SCL_qgMJRwgEb4Qi, turn 3
   Root cause: RUNBOOK Path 4 didn't list "referral" as transfer-worthy. Agent interpreted "a referral" as wanting to schedule a "referral appointment."

2. **Stale availability cascade** — SCL_B8MWYb2dfrf5, turns 44-47
   Root cause: book_appt description said "retry once" but didn't address refreshing stale get_availability data after multiple consecutive failures.

## Changes Made
1. RUNBOOK.md: Added "Referral requests" to Path 4 transfer list + Step 1 path description
   Why: Agent must recognize referrals as out-of-scope and transfer immediately

2. tools.ts (book_appt): Clarified retry behavior — try next slot, then re-check availability after 2 consecutive failures
   Why: Prevents cascading failures from stale slot data

## Skipped Issues
- **Double transfer_call**: Persists in 6+ calls (SCL_aQJVHVT7yu9s, SCL_7eNUiNAGuNAX, SCL_a3aJmtBwS9qr, SCL_FiTuW8kYQgYW, SCL_ZRH9zcisbPPw, SCL_qgMJRwgEb4Qi). Code-level race condition, not prompt-fixable.
- **STT quality issues**: Garbled input in several calls. Not prompt-fixable.
- **"Speak to someone" during frustration (SCL_R8bZsBSdFH9K, turn 42)**: Caller was frustrated about interruptions, not explicitly requesting a human. Call completed successfully. Adding "speak to someone" as a trigger risks false positives.
