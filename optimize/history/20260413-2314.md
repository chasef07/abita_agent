# Optimization Run: 2026-04-13

## Transcripts Evaluated
- SCL_yCikzGUJg8Qp: existing_patient (FAQ — confirm appt + address), clean
- SCL_nu6wr8TtYVoJ: existing_patient (confirm appt — none found), clean
- SCL_wm4GD27oitnH: new_patient → transfer, transfer-on-insistence delayed (18 turns)
- SCL_NM6DAPgpdzRL: new_patient, clean — successful registration + booking (31 turns, complex name with interpreter)
- SCL_z3ashfDYJ8Py: transfer (surgery questions), clean
- SCL_gAnP2AQui2xj: transfer (caller tried AI before), clean
- SCL_sqzfRD9iU4xJ: new_patient, clean — successful registration + booking (28 turns)
- SCL_rgb2MvzJJJj7: transfer (specific person + glasses), clean
- SCL_xstNMuRQFqVG: new_patient → transfer, confusion loop on email + transfer-on-insistence delayed (39 turns)
- SCL_aohfx9T427Rg: transfer (surgery coordination), clean
- SCL_mT8GaruV4hWB: transfer (caller insists), agent pushed back 2x before transfer
- SCL_5tJgWjUa436C: new_patient, email correction confusion + caller discovered AI (21 turns, cut off)
- SCL_DCSmrdc9gdnU: new_patient, member ID correction loop — agent read back wrong ID 3x (23 turns)
- SCL_t8TJAJ6Q5FN7: incomplete — caller spoke Italian/Spanish, hung up after 4 turns
- SCL_Hu9Up4UFiFbA: new_patient → transfer, Spanish handling failure + phone number collection loop (25 turns)
- SCL_Cta8p6vxVZh2: existing_patient → transfer, 8x get_availability calls including duplicates (9 turns)
- SCL_wbvb8AfcHWoY: existing_patient, clean — booked appointment (21 turns, chatty caller)
- SCL_o4DnRQeTEPpc: faq (insurance check), clean (7 turns)
- SCL_JXnXmSrddSkH: transfer (prescription to Walgreens), clean
- SCL_RCfzXo38taz8: transfer (doctor's office calling), clean

## Issues Found
1. **Transfer-on-insistence still broken** — SCL_wm4GD27oitnH (turn 18), SCL_xstNMuRQFqVG (turns 37-39), SCL_mT8GaruV4hWB (turns 2-4)
   Root cause: Path 4 allowed two stages (ask + scheduling pushback), creating an effective double pushback. Agent took 3-4 turns to transfer after first human request.

2. **Duplicate/redundant get_availability calls** — SCL_Cta8p6vxVZh2 (8 calls, some duplicated dates)
   Root cause: LLM generates parallel tool calls; no instruction to limit to one per turn. Also, agent didn't recognize that repeated searches returning the same date means that IS the earliest.

3. **Read-back correction loop** — SCL_DCSmrdc9gdnU (turns 17-21)
   Root cause: When caller said member ID was wrong, agent tried to re-interpret instead of asking caller to repeat the item from scratch. Read back same wrong value 3 times.

## Changes Made
1. RUNBOOK.md (line 11 + Path 4): Simplified transfer-on-insistence — removed intermediate scheduling pushback. One ask, then transfer.
   Why: Two-stage pattern (ask what + push back if scheduling) = effective double pushback in practice.

2. tools.ts (get_availability description): Added "Only call once per turn — never in parallel. If returned date = already offered, stop."
   Why: LLM was generating 2-8 parallel availability calls, including duplicates, causing noticeable delays.

3. RUNBOOK.md (Path 2, step 8): Added "If caller says any item is wrong, ask them to say just that item again from scratch."
   Why: Agent was guessing at corrections instead of re-collecting, leading to 3x wrong read-backs.

## Skipped Issues
- Spanish handling failure (SCL_Hu9Up4UFiFbA): Caller asked for Spanish, agent started in Spanish then switched to English. SOUL.md already has a clear rule. Root cause is LLM/STT interpretation error — adding more prompt text won't help.
- Email collection confusion loops (SCL_xstNMuRQFqVG turns 29-36, SCL_5tJgWjUa436C turns 11-13): STT struggles with dictated email addresses. The prompt already says no email read-back. These are fundamentally STT accuracy issues.
- AI disclosure causing trust loss (multiple calls): SOUL.md policy requires honesty about AI identity. This is by design.
