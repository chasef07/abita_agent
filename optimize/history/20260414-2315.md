# Optimization Run: 2026-04-14

## Transcripts Evaluated
- SCL_wvQYAX5vtEvW: new_patient (Spanish), confusion loop — member ID collection (25 turns) + stuck "D-R-I-V-E" spelling loop (25 turns), 111 turns total
- SCL_Hh2mB8wyunCg: new_patient x2 (Spanish), clean — 2 patients registered + booked, impressive handling (94 turns)
- SCL_ZiJjyT5Hze6V: faq (insurance), communication barrier — caller couldn't state insurance name (64 turns, not fixable)
- SCL_ZXxDExEdBtAu: new_patient, clean — registration + booking (33 turns)
- SCL_tvoLGGD2RxQo: new_patient → transfer, insurance plan name mismatch (31 turns, backend issue)
- SCL_8wSr7b46xq2z: new_patient, clean — cataract consultation (27 turns)
- SCL_HD3eB9yzibap: new_patient, Oscar HMO → Oscar Health mapping issue (22 turns, LLM error)
- SCL_gzzHubE8Z96W: new_patient, clean — patient already existed, agent recovered (20 turns)
- SCL_kYF5sq5vk8su: test call, skipped (15 turns)
- SCL_iv57Za86qzDA: existing_patient, clean — reschedule (10 turns)
- SCL_Bhfcit3rAWv8: existing_patient, clean — verification + scheduling (13 turns)
- SCL_pvHcDK2HvvHh: faq → new_patient, cut off after odd comment (8 turns)
- SCL_PdZMqdFiUbg8: existing_patient/faq, clean — address lookup (9 turns)
- SCL_UNaM4EZnz5rS: faq, clean — services info (3 turns)
- SCL_TRYa2gUu4qsD: transfer, **transfer-on-insistence broken** — "Operator" 5x before transfer (7 turns)
- SCL_Bvq2PDycTd9Q: faq, clean — insurance not accepted (7 turns)
- SCL_AD8M7szUTGd6: faq, clean — insurance check + scheduling start (6 turns)
- SCL_r6eSzZdb8o22: transfer (surgery), clean (3 turns)
- SCL_LFwTXXVvbUvS: transfer, **transfer-on-insistence broken** — "Operator" 4x before transfer (4 turns)
- SCL_j6kcCEYMvMCh: wrong number, clean (4 turns)

## Issues Found
1. **Transfer-on-insistence still broken** — SCL_TRYa2gUu4qsD (turns 2-7), SCL_LFwTXXVvbUvS (turns 2-5)
   Root cause: "operator" and "receptionist" are not in the trigger word list. Additionally, the scheduling pushback in the rule ("I can book appointments right now") creates an effective double pushback. Previous fix (from 2026-04-13 run) was not merged to main.

2. **Stuck repetition loop** — SCL_wvQYAX5vtEvW (turns 85-109)
   Root cause: Agent repeated "D-R-I-V-E" identically 15+ times when Spanish-speaking caller asked for a different spelling approach. No rule tells the agent to vary its approach after repeated failures.

## Changes Made
1. RUNBOOK.md (line 11 + Path 4): Added "operator" and "receptionist" to trigger words. Removed scheduling pushback. Simplified to: one ask, then transfer — no exceptions, no pushback.
   Why: Two calls show callers saying "operator" 5+ times without triggering transfer because the word wasn't in the list.

2. RUNBOOK.md (General Rules): Added "break stuck loops" rule — never repeat the same phrase a third time, rephrase or try a different approach.
   Why: 25 turns of identical "D-R-I-V-E" spelling in SCL_wvQYAX5vtEvW.

3. RUNBOOK.md (Path 2, step 8): Added correction handling — if caller says an item is wrong, ask them to say just that item again from scratch.
   Why: Carries forward unmerged fix from 2026-04-13 run (SCL_DCSmrdc9gdnU).

4. tools.ts (get_availability): Added "Only call once per turn — never in parallel. If returned date = already offered, stop."
   Why: Carries forward unmerged fix from 2026-04-13 run (SCL_Cta8p6vxVZh2). Also seen in SCL_8YdbfMTHixyk (3 parallel calls).

5. replay.test.ts: Added new test for "operator" trigger word. Updated existing transfer test history to match new prompt behavior.
   Why: Validates that "operator" is now recognized as a trigger word.

## Skipped Issues
- Member ID confusion loop (SCL_wvQYAX5vtEvW turns 36-61): Primarily STT/language barrier. Agent's read-back pattern contributes but existing VOICE.md rules already cover this. Adding more text won't help.
- Communication barrier (SCL_ZiJjyT5Hze6V): Severe STT issue with multilingual elderly caller. Not fixable by prompt.
- Oscar HMO → Oscar Health (SCL_HD3eB9yzibap): LLM error — add_patient description already says to use exact plan name from accepted list. Instructions are clear; LLM didn't follow them.
- Insurance plan name mismatch (SCL_tvoLGGD2RxQo): Backend issue — "Choice Plus" not recognized. Not a prompt issue.
